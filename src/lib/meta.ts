/** Build facts shown in the verified indicator. Test count is asserted in tests/meta.test.ts. */
export const TESTS = 170;
export const VERSION = '1.1.0';
export const BUILT = typeof __BUILD_DATE__ === 'string' ? __BUILD_DATE__ : '';
